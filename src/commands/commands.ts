import Config from './../core/Config'

export class Commands {
    public static open_workbench = `${Config.extensionName}.open-workbench`

    public static replace_with = `${Config.extensionName}.replace-with`

    public static config_call_function_name = `${Config.extensionName}.config-call-function-name`

    public static config_locales = `${Config.extensionName}.config-locales`

    public static config_locales_auto = `${Config.extensionName}.config-locales-auto`
}
